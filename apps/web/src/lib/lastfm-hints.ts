export const LASTFM_TIMING_HINT = "Last.fm has no exact position or pause events. Progress is estimated. Detected gaps up to 2 minutes can resume the estimate; seeks and same-song restarts cannot be detected. Longer gaps, 15 seconds without usable updates, or the track's end hold the timer. Missing duration leaves the bar empty. Visible widgets wait 1 second between requests, plus response time; hidden tabs wait 5 seconds. Errors retry up to every 10 seconds without a widget error message.";
export const LASTFM_PAUSE_HINT = "The pause display follows Last.fm's now-playing flag, not your player's pause button. If that flag stays on, a pause cannot be detected. An outage keeps the last display while requests retry; it does not trigger pause mode.";
